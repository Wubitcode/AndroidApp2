<?xml version="1.0" encoding="utf-8"?>

<!--
    Launch screen for the Tim Hortons coffee application.

    The splash screen introduces the application before
    automatically navigating to the main coffee menu.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#C8102E"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="32dp">

    <!--
        Temporary application image.
        This can later be replaced with a dedicated coffee logo.
    -->
    <ImageView
        android:layout_width="130dp"
        android:layout_height="130dp"
        android:contentDescription="@string/app_logo_description"
        android:src="@mipmap/ic_launcher" />

    <!-- Displays the application title. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:text="@string/splash_title"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:textStyle="bold" />

    <!-- Displays a short application description. -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="@string/splash_subtitle"
        android:textColor="#FFFFFF"
        android:textSize="17sp" />

</LinearLayout>